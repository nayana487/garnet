<h2>Users</h2>
<table>
  <% @users.each do |user| %>
    <tr>
      <td><%= avatar(user) %></td>
      <td><%= link_to user.name, user %></td>
      <td><%= link_to user.username, user.github_id ? user.gh_url : user %></td>
    </tr>
  <% end %>
</table>
