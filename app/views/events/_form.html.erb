<%= form_for ((defined? group) ? [group, event] : event) do |f| %>
  <%= f.text_field :title, placeholder: "Title" %>
  <div class="date"><%= f.datetime_select :date %></div>
  <%= f.submit "Save event" %>
<% end %>
